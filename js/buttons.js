let elementos = document.getElementsByClassName("botoncito");

for (let i = 0; i < elementos.length; i++) {
    const element = elementos[i];
    console.log("hola");

    // Realiza acciones con cada elemento aquí
    console.log(element);
  }